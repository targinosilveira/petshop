<h1>Listando Animais</h1>

<h3>Cliente: <%= @cliente.nome %></h3>

<%= link_to 'Novo', new_animal_path({:cliente => @cliente.id}) %><br /><br />

<table>
  <tr>
    <th>Nome</th>
  </tr>

<% @animais.each do |animal| %>
  <tr>
    <td><%=h animal.nome %></td>
    <td><%= link_to 'Show', animal %></td>
    <td><%= link_to 'Edit', edit_animal_path(animal) %></td>
    <td><%= link_to 'Destroy', animal, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo', new_animal_path %>